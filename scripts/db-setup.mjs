import fs from "node:fs/promises";import path from "node:path";import postgres from "postgres";if(!process.env.DATABASE_URL){console.error("DATABASE_URL est requis.");process.exit(1);}const sql=postgres(process.env.DATABASE_URL,{max:1,prepare:false});async function apply(file){const content=await fs.readFile(file,"utf8");process.stdout.write(`Application ${path.relative(process.cwd(),file)}... `);await sql.unsafe(content);console.log("OK");}try{await apply(path.join(process.cwd(),"db","schema.sql"));const migrationDir=path.join(process.cwd(),"db","migrations");const migrations=(await fs.readdir(migrationDir)).filter(name=>name.endsWith(".sql")).sort();for(const name of migrations)await apply(path.join(migrationDir,name));console.log("Base prête.");}finally{await sql.end({timeout:5});}
