import { notFound } from "next/navigation";
import { sql } from "@/lib/db";
import { JoinForm } from "@/components/join-form";
export default async function JoinPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const [r]=await sql`select e.name,e.logo_url,e.primary_color,p.program_name,p.mode,p.reward_threshold,p.reward_label from establishments e join loyalty_programs p on p.establishment_id=e.id where e.slug=${slug} and e.status='active' and p.active=true`;if(!r)notFound();return <main className="auth-wrap" style={{background:`linear-gradient(160deg, ${r.primary_color}18, #f5f6f8 55%)`}}><section className="card auth-card">{r.logo_url&&<img src={r.logo_url} alt="" style={{width:64,height:64,objectFit:"contain",borderRadius:14}}/>}<span className="eyebrow" style={{marginTop:12}}>{r.mode==="STAMPS"?"Carte à tampons":"Carte à points"}</span><h2 style={{margin:"14px 0 6px"}}>{r.name}</h2><p className="muted">{r.program_name} · {r.reward_threshold} unités = {r.reward_label}</p><JoinForm slug={slug}/></section></main>}
