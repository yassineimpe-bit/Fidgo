import { getSession } from "@/lib/auth";
import { sql } from "@/lib/db";
import { canManageStaff } from "@/lib/loyalty";
import { rejectCrossOrigin } from "@/lib/security";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const originError=rejectCrossOrigin(req);if(originError)return originError;const session=await getSession();if(!session)return Response.json({error:"UNAUTHORIZED"},{status:401});if(!canManageStaff(session.role))return Response.json({error:"FORBIDDEN"},{status:403});const {id}=await params;if(id===session.staffId)return Response.json({error:"CANNOT_DISABLE_SELF"},{status:409});const body=await req.json();if(typeof body.active!=="boolean")return Response.json({error:"INVALID_INPUT"},{status:400});const [target]=await sql`select id,role from staff_users where id=${id} and establishment_id=${session.establishmentId} limit 1`;if(!target)return Response.json({error:"NOT_FOUND"},{status:404});if(target.role==="OWNER"||target.role==="MANAGER")return Response.json({error:"PROTECTED_ROLE"},{status:403});const rows=await sql`update staff_users set active=${body.active},updated_at=now() where id=${id} and establishment_id=${session.establishmentId} returning id,email,role,active,created_at`;return Response.json(rows[0]);}
