import { getSession } from "@/lib/auth";
import { sql } from "@/lib/db";
export async function GET(){const session=await getSession();if(!session)return Response.json({error:"UNAUTHORIZED"},{status:401});const [stats]=await sql`select (select count(*)::int from customers where establishment_id=${session.establishmentId} and deleted_at is null) as customers,(select count(*)::int from cards where establishment_id=${session.establishmentId} and active=true) as active_cards,(select coalesce(sum(case when delta>0 then delta else 0 end),0)::int from transactions where establishment_id=${session.establishmentId}) as units_issued,(select count(*)::int from transactions where establishment_id=${session.establishmentId} and type='redeem') as rewards_redeemed,(select count(distinct card_id)::int from transactions where establishment_id=${session.establishmentId} and created_at>=now()-interval '7 days') as active_this_week,(select count(*)::int from campaigns where establishment_id=${session.establishmentId} and status='sent') as campaigns_sent`;return Response.json(stats,{headers:{"cache-control":"no-store"}});}
